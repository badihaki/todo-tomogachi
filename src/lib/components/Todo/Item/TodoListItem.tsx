import ITodoItem from '@/lib/interfaces/todo/ITodoItem'
import React from 'react'

function TodoListItem(props:{
    listItem:ITodoItem
}) {
  return (
    <div>
      Item
    </div>
  )
}

export default TodoListItem
